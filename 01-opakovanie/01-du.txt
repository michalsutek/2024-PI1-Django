1. Vytvor program, ktorý si vypýta číslo (celé) a vypíše všetky jeho delitele
2. Vytvor program, ktorý si vypýta číslo (celé) a zistí, či je to prvočíslo
3. vytvor program, ktorý si vypýta číslo (celé) a vypíše všetky prvočísla menšia ako zadané číslo